\begin{table}[t]
\centering
\caption{toniot: horizon_sensitivity -- SKIPPED.}
\label{tab:toniot_horizon_sensitivity}
\begin{tabular}{p{0.9\linewidth}}
\toprule
\textbf{SKIPPED for toniot} \\
\midrule
instance-level gate failed at every candidate horizon: H=10: zero pre-impact forecasting instances could be constructed at any split -- every asset's observed activity fits inside a single Delta-t window (no earlier 'cut point' exists before the asset's first IMP window, and/or no later window exists to confirm a negative).; H=30: zero pre-impact forecasting instances could be constructed at any split -- every asset's observed activity fits inside a single Delta-t window (no earlier 'cut point' exists before the asset's first IMP window, and/or no later window exists to confirm a negative).; H=60: zero pre-impact forecasting instances could be constructed at any split -- every asset's observed activity fits inside a single Delta-t window (no earlier 'cut point' exists before the asset's first IMP window, and/or no later window exists to confirm a negative). \\
\bottomrule
\end{tabular}
\end{table}
